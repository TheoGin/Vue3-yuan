import { createApp } from "vue";
import App from "./App.vue";
/* import gsap from "gsap"; */

createApp(App).mount("#app");

/* let obj = {count: 1};
gsap.to(obj, {
  count: 100,
  duration: 1,
  onUpdate: () => {
    console.log(obj);
  },
});
 */